// Preload script - runs before renderer
// We don't need Node integration for this app, keeping it minimal
window.addEventListener('DOMContentLoaded', () => {
  // Any initialization that needs to happen before the app loads
});
